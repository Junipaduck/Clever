<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itwillbs.clever.mapper.GoodsMapper">

	<insert id="insertGoods">
		<selectKey keyProperty="goods_idx" resultType="int" order="BEFORE">
				SELECT IFNULL(MAX(goods_idx), 0) FROM goods
		</selectKey>
			INSERT
				INTO goods
				VALUES (
					#{goods_idx} + 1 -- 굿즈 번호
					, #{goods_name}
					, #{goods_price}
					, #{goods_content}
					, #{goods_file}
					, #{goods_file_path}
					, now()
				)
	</insert>
	
	<!-- 굿즈 목록 조회 -->
	<select id="selectGoodsList" resultType="com.itwillbs.clever.vo.GoodsVO">
		SELECT *
			FROM goods
	</select>
	
	
</mapper>






















